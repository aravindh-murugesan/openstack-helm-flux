apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
configMapGenerator:
  - name: openstack-helm-flux-cinder-values
    namespace: openstack
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
  labels:
    openstack-helm-flux/component-group: openstack
