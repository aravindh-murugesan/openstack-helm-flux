---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openstack

configMapGenerator:
  - name: openstack-helm-flux-memcached-values-override
    behavior: create
    options:
      disableNameSuffixHash: true
      labels:
        reconcile.fluxcd.io/watch: Enabled
    files:
      - values.yaml=value-overrides.yaml

resources:
  - ../../../../base/openstack-apps/memcached
